# 企业园区车辆移动授权

`POST /api/enterprise/yms/yard-movement-authorization` 在车辆入园、移库或靠台前检查身份、车辆资质、月台容量、安全检查、危险品许可、货运单证和审计证据。

- `AUTHORIZE`：安全和运营条件完整，可以执行车辆移动。
- `DISPATCH`：不存在安全阻断，但需调度员补齐预约、单证、月台、封签或现场确认。
- `BLOCKED`：身份、车辆、安全、容量、危险品许可或审计控制失败。

接口可接入门岗、预约、车牌识别、月台看板和消息通知，但核心授权保持确定性和可审计。
